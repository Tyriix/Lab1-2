@model ICollection<Lab1_2.Models.BlogItem>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<h1>Lista wpisów</h1>

<table class="table">
    
        @foreach (var item in @Model)
        {
            <tr>
            <td>@item.Title</td>
            <td>@item.Content</td>
            <td>@item.CreationTimstamp</td>
            </tr>
        }
    
</table>
